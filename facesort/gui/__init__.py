"""FaceSort GUI: a polished web front-end (React) hosted in a native macOS
window via pywebview. The Python side (api.py) bridges the browser to the same
core pipeline the CLI uses; app.py opens the window."""
